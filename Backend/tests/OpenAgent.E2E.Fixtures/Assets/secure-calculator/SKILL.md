---
name: secure-calculator
description: Adds two integers with an isolated Python script and reports the sandbox result.
---

# Secure calculator

When the user asks to verify this Skill, run `scripts/calculate.py` with the two integers as string arguments.
Read `resources/formula.md` if you need the expected formula.
